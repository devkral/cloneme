
+make EDITOR fetch from main system working (see gui.cc)
+rename name (converter and copyuser)
+split shellfile into bootloader specific installer e.g. grub, syslinux, lilo and adduserdialog and the normal part 
+offer some special options for ssds/USB-flashdrives
+automount: split the mount functions off the skript and use them whenever a new directory/file is selected
+somehow chroot on target system has no root rights (maybe ubutuntu specific)

+make things running more stable

less important:
+rework system2 output

not very important:
loop other things than raw
maybe detect via grub-probe or parted if nbd is mounted

nbd stuff
http://atrey.karlin.mff.cuni.cz/~pavel/nbd/nbd.html

try qemu (relevant files):
http://www.netzkompetenz.ch/ubuntu/qcow2-disk-image-mounten/

qcow2 Disk Image mounten

Kurzanleitung um eine gcow2 Disk-Image auf Ubuntu LTS 10.04 zu mounten:

Als root, mit einem qcow2 image

for partitions
modprobe nbd max_part=8
qemu-nbd --connect=/dev/nbd0 db1_old.qcow2
# mount first partition
mount /dev/nbd0p1 /mnt/

wieder freigeben:

umount /mnt/
qemu-nbd --disconnect /dev/nbd0



http://blog.loftninjas.org/2008/10/27/mounting-kvm-qcow2-qemu-disk-images/
