this bash script can clone a system to an usbstick or whatever (am existing partition is required and just one partition can be copied)
It can also update an existing system via rsync. 

cloneme commandline version is in src
